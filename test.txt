        <Provider style={{backgroundColor:'black'}}>
            <View>
            <Button onPress={showDialog}>Choose Section</Button>
            <Portal>
                <Dialog visible={visible} onDismiss={hideDialog}>
                <Dialog.Title>Choose</Dialog.Title>
                <Dialog.Content>
                    <RadioButton.Group>
                        {listSections.map((sect, key)=>(
                            <RadioButton.Item label={sect.name} value={sect.name} onPress={() => addSection(sect.name)} />
                        ))}
                    </RadioButton.Group>
                    <Button Title='salut'/>
                </Dialog.Content>
                <Dialog.Actions>
                    <Button onPress={hideDialog}>Done</Button>
                </Dialog.Actions>
                </Dialog>
            </Portal>
            </View>
      </Provider>
    );